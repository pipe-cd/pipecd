# This configures a non-progressive delivery.
# This means the new version is applied directly to PRIMARY variant.
apiVersion: pipecd.dev/v1beta1
kind: KubernetesApp
spec:
  input:
    kustomizeVersion: 3.5.5
