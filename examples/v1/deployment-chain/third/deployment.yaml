apiVersion: apps/v1
kind: Deployment
metadata:
  name: dchain-third
  labels:
    app: dchain-third
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dchain-third
      pipecd.dev/variant: primary
  template:
    metadata:
      labels:
        app: dchain-third
        pipecd.dev/variant: primary
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      containers:
      - name: helloworld
        image: ghcr.io/pipe-cd/helloworld:v0.32.0
        args:
          - server
        ports:
        - containerPort: 9085
