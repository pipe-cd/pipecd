apiVersion: pipecd.dev/v1beta1
kind: KubernetesApp
spec:
  name: simple
  plugins:
    kubernetes_multicluster:
      input:
        manifests:
          - deployment.yaml
          - service.yaml
        kubectlVersion: 1.32.2
      trafficRouting:
        method: podselector
      service:
        name: simple
