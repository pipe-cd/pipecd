apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: traffic-test-vs
spec:
  hosts:
  - traffic-test
  http:
  - route:
    - destination:
        host: traffic-test
        subset: primary
      weight: 100 
