apiVersion: pipecd.dev/v1beta1
kind: KubernetesApp
spec:
  pipeline:
    stages:
      - name: K8S_STAGE_OUT
        with:
          weight: 10
      - name: K8S_TRAFFIC_ROUTE
        with:
          stage: 10
          primary: 90
      - name: K8S_PRIMARY_OUT
      - name: K8S_TRAFFIC_ROUTE
        with:
          primary: 100
      - name: K8S_STAGE_IN
