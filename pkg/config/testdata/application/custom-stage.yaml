apiVersion: pipecd.dev/v1beta1
kind: LambdaApp
spec:
  pipeline:
    stages:
      - name: CUSTOM_SYNC
        desc: "deploy by sam"
        with:
          env: 
            AWS_PROFILE: default
          runs: 
            - "sam deploy -g {{ .AWS_PROFILE }}"