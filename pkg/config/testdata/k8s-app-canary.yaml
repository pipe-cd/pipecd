apiVersion: pipecd.dev/v1beta1
kind: K8sApp
spec:
  pipeline:
    stages:
      - name: K8S_CANARY_OUT
        with:
          weight: 10
        timeout: 10m
        postDelay: 1m
      - name: WAIT_APPROVAL
        with:
          approvers:
            - foo
            - bar
      - name: K8S_ROLLOUT
      - name: K8S_CANARY_IN
