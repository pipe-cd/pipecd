apiVersion: pipecd.dev/v1beta1
kind: KubernetesApp
spec:
  input:
    helmChart: git@github.com:org/config-repo.git:charts/demoapp?ref=v1.0.0
    helmValueFiles:
    - values.yaml
    helmVersion: 3.1.1
